
    Instructions:
    Using the dataset above, do the following:
      Show how to prepare the dataset into training and test sets (EDA)
      Create a baseline MLP classification model
      Evaluate your model using the dataset
      Improve the accuracy of your baseline model using known techniques. Minimum Accuracy is 85%
      Save the best model
      Test 2 sample images using the saved best model, the images should NOT come from the dataset.
      Create a function to show the effectiveness of your model.
    There should be an explanation for each bullet point,
    NOTE: Submit the link of Google Colab and the dataset used (links should be accessible, otherwise your exam will be zero)